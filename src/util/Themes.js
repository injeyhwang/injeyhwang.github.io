const sail = '#FFFCED';
const gray = '#282C33';
const black = '#16171D';

const theme = {
  light: {
    backgroundColor: sail,
    color: gray
  },
  dark: {
    backgroundColor: black,
    color: sail
  }
};

export default theme;